new MutationObserver(async () => {
  if (location.href == "https://vrchat.com/home") {
    VRChatMobileClientLogin.postMessage(e[67228]);
  }
}).observe(document, { childList: true, subtree: true });

String.prototype.join = function (n) {
  VRChatMobileClientLogin.postMessage(n);
};
